<% if user_signed_in? %>
  <%= form_with url: public_search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :method, options_for_select({"投稿" => "post", "学習項目" => "learning_item"}) %>
    <%= f.submit "検索" %>
  <% end %>
<% end %>